function Map(props: { id?: string }) {
  return (
    <div
      className="bg-white rounded text-center flex justify-center items-center"
      id={props.id}
    >
      Map
    </div>
  );
}

export default Map;
